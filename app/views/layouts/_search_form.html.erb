<%= form_tag(:action=> "index", :controller=> "home", :method=> "get", id: "search-form") do %>
        <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Pesquise"%>
        <%= select_tag(:category, options_for_select([['Serviço', 1], ['Comércio', 2]], :selected => params[:category]), prompt: 'Categoria', class: "form-control") %>
        <!-- < %= collection_select(:params, :jobs, Job.all, :id, :nome, :prompt=> 'Selecionar job') %> -->
        <%= %>
    <%= submit_tag "Pesquise", class: "btn btn-primary"%>
<% end %>