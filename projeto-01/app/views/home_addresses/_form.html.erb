<%= form_for(@home_address) do |f| %>
  <% if @home_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_address.errors.count, "error") %> prohibited this home_address from being saved:</h2>

      <ul>
      <% @home_address.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
